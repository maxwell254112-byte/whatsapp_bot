[api]
; Point to the worker API folder on your web server
base_url = http://localhost/whatsapp_bot/web/api/worker
worker_token =

[worker]
browser_channel = msedge
headless = false
profile_dir = wa_profile
screenshot_dir = screenshots
temp_dir = temp
poll_interval_seconds = 5
heartbeat_interval_seconds = 20
